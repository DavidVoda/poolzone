services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: poolzone
      POSTGRES_PASSWORD: poolzone
      POSTGRES_DB: poolzone
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
